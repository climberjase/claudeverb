---
phase: 06-playback-enhancements-eq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claudeverb/algorithms/filters.py
  - claudeverb/engine.py
  - tests/test_biquad.py
  - tests/test_engine.py
autonomous: true
requirements: [EQ-01, EQ-02, PLAY-03]

must_haves:
  truths:
    - "Biquad.low_shelf() and Biquad.high_shelf() factory methods exist and produce correct frequency response"
    - "Silence padding appends correct number of zero samples to mono and stereo audio"
    - "3-band EQ chain (low shelf + mid parametric + high shelf) applies to wet signal inside engine when enabled"
    - "EQ bypasses cleanly when disabled (wet signal unchanged)"
    - "Padded dry signal has same length as padded wet signal (no shape mismatch in blend)"
  artifacts:
    - path: "claudeverb/algorithms/filters.py"
      provides: "Biquad.low_shelf() and Biquad.high_shelf() classmethods"
      contains: "def low_shelf"
    - path: "claudeverb/engine.py"
      provides: "pad_with_silence(), apply_eq(), updated process_audio()"
      contains: "def pad_with_silence"
    - path: "tests/test_biquad.py"
      provides: "TestBiquadLowShelf, TestBiquadHighShelf test classes"
      contains: "class TestBiquadLowShelf"
    - path: "tests/test_engine.py"
      provides: "TestSilencePadding, TestEQ test classes"
      contains: "class TestSilencePadding"
  key_links:
    - from: "claudeverb/engine.py"
      to: "claudeverb/algorithms/filters.py"
      via: "Biquad.low_shelf, Biquad.high_shelf, Biquad.parametric imports"
      pattern: "Biquad\\.(low_shelf|high_shelf|parametric)"
    - from: "claudeverb/engine.py::process_audio"
      to: "claudeverb/engine.py::apply_eq"
      via: "conditional call after algo.process()"
      pattern: "apply_eq.*wet.*eq_params"
---

<objective>
Add shelving EQ filters to Biquad, silence padding utility, and 3-band EQ integration into the engine's processing pipeline.

Purpose: Provides the DSP foundation (shelf filters, silence padding, EQ chain) that the UI layer (Plan 03) will wire controls to. Without this, the UI has no backend to call.

Output: Extended Biquad class with shelf filters, engine functions for silence padding and EQ, comprehensive tests for all new DSP code.
</objective>

<execution_context>
@/Users/jasondoherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jasondoherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-playback-enhancements-eq/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From claudeverb/algorithms/filters.py (Biquad class -- existing factory pattern):
```python
class Biquad:
    __slots__ = ("b0", "b1", "b2", "a1", "a2", "d1", "d2", "sample_rate")

    def __init__(self, b0, b1, b2, a1, a2, sample_rate=48000): ...
    def process(self, block: np.ndarray) -> np.ndarray: ...
    def reset(self) -> None: ...
    def set_coefficients(self, b0, b1, b2, a1, a2) -> None: ...

    @classmethod
    def lowpass(cls, freq, q, sample_rate=48000) -> Biquad: ...
    @classmethod
    def highpass(cls, freq, q, sample_rate=48000) -> Biquad: ...
    @classmethod
    def parametric(cls, freq, q, gain_db, sample_rate=48000) -> Biquad: ...
    # MISSING: low_shelf, high_shelf -- this plan adds them
```

From claudeverb/engine.py (current process_audio signature):
```python
def process_audio(algorithm_name: str, params: dict, audio: np.ndarray) -> dict:
    """Returns dict with keys: dry, wet, ir, metrics, figures"""

def blend_wet_dry(dry: np.ndarray, wet: np.ndarray, mix_pct: int) -> np.ndarray: ...
```

From claudeverb/config.py:
```python
SAMPLE_RATE = 48000
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shelf filter methods to Biquad and silence padding to engine</name>
  <files>claudeverb/algorithms/filters.py, claudeverb/engine.py, tests/test_biquad.py, tests/test_engine.py</files>
  <behavior>
    - TestBiquadLowShelf: low_shelf(200, 6.0) boosts frequencies below 200 Hz by ~6dB (measure at 50 Hz vs 10 kHz)
    - TestBiquadLowShelf: low_shelf(200, -6.0) cuts frequencies below 200 Hz by ~6dB
    - TestBiquadLowShelf: low_shelf(200, 0.0) passes signal unchanged (unity gain)
    - TestBiquadHighShelf: high_shelf(6000, 6.0) boosts frequencies above 6 kHz by ~6dB
    - TestBiquadHighShelf: high_shelf(6000, -6.0) cuts frequencies above 6 kHz by ~6dB
    - TestBiquadHighShelf: high_shelf(6000, 0.0) passes signal unchanged (unity gain)
    - TestSilencePadding: pad_with_silence(mono, 2.0) returns array with length = original + 96000
    - TestSilencePadding: pad_with_silence(stereo, 2.0) returns (2, original+96000) shape
    - TestSilencePadding: pad_with_silence(mono, 0.0) returns original array unchanged
    - TestSilencePadding: padded region is all zeros, dtype is float32
  </behavior>
  <action>
    **Tests first (RED):**

    In `tests/test_biquad.py`, add two test classes:
    - `TestBiquadLowShelf`: Test frequency response by processing a long white noise signal through the filter, then comparing FFT magnitude at 50 Hz (below shelf) vs 10 kHz (above shelf). For +6dB shelf at 200 Hz, the ratio should be ~4x (6dB) at 50 Hz vs passband. Also test 0dB gain produces unity output.
    - `TestBiquadHighShelf`: Same approach but verify boost/cut above shelf frequency.

    In `tests/test_engine.py`, add `TestSilencePadding` class:
    - Test mono padding length, stereo padding shape, zero-second padding passthrough, dtype preservation, and that the appended region is all zeros.

    **Implementation (GREEN):**

    In `claudeverb/algorithms/filters.py`, add two classmethods to Biquad:
    - `low_shelf(cls, freq, gain_db, q=0.707, sample_rate=48000)` using Bristow-Johnson shelf formula from RESEARCH.md
    - `high_shelf(cls, freq, gain_db, q=0.707, sample_rate=48000)` using Bristow-Johnson shelf formula from RESEARCH.md

    Both follow exact same pattern as existing factory methods: compute coefficients, return `cls(b0/a0, b1/a0, b2/a0, a1/a0, a2/a0, sample_rate)`.

    In `claudeverb/engine.py`, add `pad_with_silence(audio, seconds, sample_rate=48000)`:
    - Append `int(seconds * sample_rate)` zeros to audio
    - Handle mono (N,) and stereo (2, N) shapes
    - Return original if seconds <= 0
    - Use np.zeros + np.concatenate, dtype float32
  </action>
  <verify>
    <automated>cd /Users/jasondoherty/Documents/SRC/claudeverb && python -m pytest tests/test_biquad.py::TestBiquadLowShelf tests/test_biquad.py::TestBiquadHighShelf tests/test_engine.py::TestSilencePadding -x -v</automated>
  </verify>
  <done>Biquad has low_shelf/high_shelf classmethods that produce correct frequency response. pad_with_silence correctly pads mono and stereo audio. All new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 3-band EQ chain to engine with enable/disable</name>
  <files>claudeverb/engine.py, tests/test_engine.py</files>
  <behavior>
    - TestEQ: apply_eq with all gains at 0dB returns signal approximately unchanged
    - TestEQ: apply_eq with low_gain=+6dB boosts low frequencies measurably
    - TestEQ: apply_eq processes stereo (2, N) correctly, output shape matches input
    - TestEQ: apply_eq output is clipped to [-1, 1] and dtype is float32
    - TestEQ: process_audio with eq_params=None produces same result as without EQ
    - TestEQ: process_audio with eq_params enabled processes through EQ chain
    - TestEQ: process_audio with silence_seconds > 0 pads input, dry and wet have same length
  </behavior>
  <action>
    **Tests first (RED):**

    In `tests/test_engine.py`, add `TestEQ` class testing:
    - `apply_eq()` with 0dB gains is near-unity (RMS difference < 0.01)
    - `apply_eq()` with +6dB low shelf measurably boosts low frequencies
    - `apply_eq()` handles stereo input, preserves shape
    - `apply_eq()` clips output to [-1, 1], preserves float32 dtype
    - `process_audio()` with `eq_params=None` works as before (backward compatible)
    - `process_audio()` with `eq_params={"enabled": True, "low_freq": 200, "low_gain": 0, "mid_freq": 1000, "mid_gain": 0, "mid_q": 1.5, "high_freq": 6000, "high_gain": 0}` processes without error
    - `process_audio()` with `silence_seconds=1.0` returns dry and wet with matching increased length

    **Implementation (GREEN):**

    In `claudeverb/engine.py`, add `apply_eq(wet, eq_params)` function:
    - Creates 3 Biquad instances: `Biquad.low_shelf(low_freq, low_gain)`, `Biquad.parametric(mid_freq, mid_q, mid_gain)`, `Biquad.high_shelf(high_freq, high_gain)`
    - Processes mono: chain all three sequentially
    - Processes stereo: for each channel, reset all filters, process channel through chain
    - Clip to [-1, 1] and cast to float32

    Modify `process_audio()` signature to accept two optional kwargs:
    - `silence_seconds: float = 0.0` -- pad input before processing
    - `eq_params: dict | None = None` -- EQ settings (dict with "enabled" key)

    Integration in `process_audio()`:
    1. If `silence_seconds > 0`: `audio = pad_with_silence(audio, silence_seconds)`
    2. Existing processing (algo.process, IR, metrics, figures) -- note: dry is now the padded audio
    3. After `wet = algo.process(audio.copy())`: if `eq_params` and `eq_params.get("enabled")`: `wet = apply_eq(wet, eq_params)`
    4. Return dict as before, with `dry` being the (possibly padded) input

    IMPORTANT: The `dry` in the return dict must be the padded audio (same length as wet). This ensures `blend_wet_dry()` gets matching shapes. Also store `original_length` (pre-padding length) in the return dict so the UI can mark the padding boundary on waveforms.
  </action>
  <verify>
    <automated>cd /Users/jasondoherty/Documents/SRC/claudeverb && python -m pytest tests/test_engine.py::TestEQ -x -v</automated>
  </verify>
  <done>apply_eq() processes 3-band EQ on wet signal. process_audio() accepts silence_seconds and eq_params kwargs, integrates both into pipeline. Backward compatible (existing tests still pass). EQ bypasses when disabled or params are None.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/jasondoherty/Documents/SRC/claudeverb && python -m pytest tests/test_biquad.py tests/test_engine.py -v
```
All existing tests still pass (backward compatibility). All new test classes pass.
</verification>

<success_criteria>
- Biquad.low_shelf() and Biquad.high_shelf() produce measurably correct frequency response
- pad_with_silence() handles mono, stereo, and zero-second edge cases
- apply_eq() processes 3-band EQ chain with clipping
- process_audio() accepts silence_seconds and eq_params without breaking existing callers
- Full test suite green: `pytest tests/ -x -q`
</success_criteria>

<output>
After completion, create `.planning/phases/06-playback-enhancements-eq/06-01-SUMMARY.md`
</output>
