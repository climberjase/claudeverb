---
phase: 09-signal-flow-diagrams-c-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claudeverb/export/__init__.py
  - claudeverb/export/dot_builder.py
  - claudeverb/algorithms/base.py
  - claudeverb/algorithms/freeverb.py
  - claudeverb/algorithms/dattorro_plate.py
  - claudeverb/algorithms/fdn_reverb.py
  - claudeverb/algorithms/dattorro_single_loop.py
  - claudeverb/algorithms/dattorro_triple_diffuser.py
  - claudeverb/algorithms/dattorro_asymmetric.py
  - claudeverb/algorithms/room_base.py
  - tests/test_signal_flow.py
autonomous: true
requirements:
  - VIZ-01

must_haves:
  truths:
    - "Every registered algorithm returns a valid DOT string from to_dot('block', params)"
    - "Every registered algorithm returns a valid DOT string from to_dot('component', params)"
    - "DOT strings contain live parameter values from the params dict"
    - "Block-level diagrams have 5-10 nodes; component-level have 15-30 nodes"
  artifacts:
    - path: "claudeverb/export/dot_builder.py"
      provides: "Shared DOT graph construction helpers (node styling, edge creation, subgraph grouping)"
      min_lines: 40
    - path: "tests/test_signal_flow.py"
      provides: "Test coverage for to_dot() on all 9 algorithms at both detail levels"
      min_lines: 60
    - path: "claudeverb/algorithms/base.py"
      provides: "to_dot() method signature on abstract base class"
      contains: "def to_dot"
  key_links:
    - from: "claudeverb/algorithms/*.py"
      to: "claudeverb/export/dot_builder.py"
      via: "import dot_builder helpers for node/edge styling"
      pattern: "from claudeverb\\.export\\.dot_builder import"
    - from: "tests/test_signal_flow.py"
      to: "claudeverb/algorithms/__init__.py"
      via: "iterate ALGORITHM_REGISTRY to test all algorithms"
      pattern: "ALGORITHM_REGISTRY"
---

<objective>
Add signal-flow diagram generation to all 9 reverb algorithms via `to_dot()` methods that return Graphviz DOT strings.

Purpose: Enables users to visualize algorithm topology at two detail levels (block and component), with live parameter values in labels. This is the data layer for VIZ-01; UI integration comes in Plan 03.

Output: `to_dot()` method on all algorithms, shared `dot_builder.py` helpers, full test coverage.
</objective>

<execution_context>
@/Users/jasondoherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jasondoherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-signal-flow-diagrams-c-export/09-CONTEXT.md
@.planning/phases/09-signal-flow-diagrams-c-export/09-RESEARCH.md

@claudeverb/algorithms/base.py
@claudeverb/algorithms/__init__.py
@claudeverb/algorithms/freeverb.py
@claudeverb/algorithms/dattorro_plate.py
@claudeverb/algorithms/fdn_reverb.py
@claudeverb/algorithms/dattorro_single_loop.py
@claudeverb/algorithms/room_base.py
@claudeverb/algorithms/dattorro_triple_diffuser.py
@claudeverb/algorithms/dattorro_asymmetric.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From claudeverb/algorithms/base.py:
```python
class ReverbAlgorithm(ABC):
    @property
    @abstractmethod
    def param_specs(self) -> dict: ...
```

From claudeverb/algorithms/__init__.py:
```python
ALGORITHM_REGISTRY = {
    "freeverb": Freeverb,
    "dattorro_plate": DattorroPlate,
    "fdn": FDNReverb,
    "dattorro_single_loop": DattorroSingleLoop,
    "dattorro_triple_diffuser": DattorroTripleDiffuser,
    "dattorro_asymmetric": DattorroAsymmetric,
    "small_room": SmallRoom,
    "large_room": LargeRoom,
    "chamber": Chamber,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dot_builder helpers and add to_dot() to base class</name>
  <files>claudeverb/export/__init__.py, claudeverb/export/dot_builder.py, claudeverb/algorithms/base.py, tests/test_signal_flow.py</files>
  <action>
1. Create `claudeverb/export/__init__.py` (empty or minimal).

2. Create `claudeverb/export/dot_builder.py` with shared DOT helpers:
   - `dsp_node(name, label, shape="box", style="rounded")` -> DOT node string with consistent styling
   - `io_node(name, label)` -> DOT node string with shape=ellipse for input/output
   - `edge(src, dst, label=None, style=None)` -> DOT edge string
   - `subgraph(name, label, nodes_dot)` -> DOT cluster subgraph string for grouping DSP stages
   - `digraph_wrap(name, body, rankdir="LR")` -> wraps body in `digraph Name { ... }` with fontname="Arial" default
   - Use consistent color scheme: input/output nodes in light blue, DSP blocks in light yellow, feedback paths as dashed red edges

3. Add `to_dot()` to `ReverbAlgorithm` base class in `base.py` as a non-abstract method with a default implementation that returns a simple "not implemented" placeholder graph. Signature: `def to_dot(self, detail_level: str = "block", params: dict | None = None) -> str`. This allows algorithms without custom to_dot() to still render something.

4. Create `tests/test_signal_flow.py` with tests:
   - `test_all_algorithms_have_to_dot()`: iterate ALGORITHM_REGISTRY, instantiate each, call `to_dot("block")` and `to_dot("component")`, assert returns non-empty string
   - `test_dot_contains_digraph()`: verify returned strings contain "digraph" keyword
   - `test_dot_block_level_node_count()`: parse DOT output, count nodes, assert 5-10 for block level
   - `test_dot_component_level_node_count()`: parse DOT output, count nodes, assert 15-30 for component level
   - `test_dot_contains_param_values()`: pass custom params dict, verify values appear in DOT string labels
   - Use a simple regex count of node definitions (lines with `[label=`) for node counting

   Note: These tests will initially fail for algorithms without custom to_dot() (they'll get the base class placeholder). That's expected -- they pass once Task 2 adds the implementations.
  </action>
  <verify>
    <automated>cd /Users/jasondoherty/Documents/SRC/claudeverb/.claude/worktrees/jovial-leakey && python -c "from claudeverb.export.dot_builder import digraph_wrap, dsp_node; print(digraph_wrap('Test', dsp_node('n1', 'Node')))"</automated>
  </verify>
  <done>dot_builder.py exports helper functions, base.py has to_dot() default, test_signal_flow.py exists with 5+ test cases</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add to_dot() to all 9 algorithm classes</name>
  <files>claudeverb/algorithms/freeverb.py, claudeverb/algorithms/dattorro_plate.py, claudeverb/algorithms/fdn_reverb.py, claudeverb/algorithms/dattorro_single_loop.py, claudeverb/algorithms/dattorro_triple_diffuser.py, claudeverb/algorithms/dattorro_asymmetric.py, claudeverb/algorithms/room_base.py</files>
  <behavior>
    - Test: Each algorithm's to_dot("block") returns DOT with 5-10 nodes
    - Test: Each algorithm's to_dot("component") returns DOT with 15-30 nodes
    - Test: Parameter values from params dict appear in DOT labels
  </behavior>
  <action>
Add `to_dot(self, detail_level, params)` to each algorithm class, using dot_builder helpers. Each algorithm has unique topology:

**Freeverb** -- block: Input -> Pre-Delay -> 8 Parallel Combs -> 4 Series Allpass -> Stereo Mix -> Output (~7 nodes). Component: expand each comb and allpass as individual nodes with feedback coefficients (~20 nodes).

**DattorroPlate** -- block: Input -> Input Diffusers -> Tank Left -> Tank Right -> Output (~6 nodes). Component: expand 4 input allpasses, 2 tank branches with individual delay/allpass/modulation nodes (~25 nodes).

**FDNReverb** -- block: Input -> Pre-Delay -> 4-Channel Delays -> Hadamard Mix -> Damping -> Output (~7 nodes). Component: expand 4 delay channels, modulation, DC blockers, individual damping filters (~22 nodes).

**DattorroSingleLoop** -- block: Input -> Input Diffusers -> Single Loop -> Output (~5 nodes). Component: expand single feedback ring with allpasses and delays (~18 nodes).

**DattorroTripleDiffuser** -- block: Input -> Triple Input Diffusers -> Tank -> Output (~6 nodes). Component: expand 6 input diffusers, tank branches (~25 nodes).

**DattorroAsymmetric** -- block: Input -> Input Diffusers -> Left Tank -> Right Tank -> Output (~7 nodes). Component: expand asymmetric tank branches with different delay lengths (~24 nodes).

**RoomReverbBase** (covers SmallRoom, LargeRoom, Chamber) -- block: Input -> Early Reflections -> Late Reverb (FDN) -> Stereo Mix -> Output (~6 nodes). Component: expand ER taps, FDN channels, diffuser allpasses per specific room type (~20-28 nodes).

For all: use `params or {k: v['default'] for k, v in self.param_specs.items()}` to get values for labels. Include key numeric values in node labels (e.g., "Delay [601 samples]", "Feedback: 0.72"). Use dot_builder helpers for consistent styling. Escape special characters in labels with backslash.

Feedback paths use dashed red edges. Signal flow is left-to-right (rankdir=LR).
  </action>
  <verify>
    <automated>cd /Users/jasondoherty/Documents/SRC/claudeverb/.claude/worktrees/jovial-leakey && pytest tests/test_signal_flow.py -x -q</automated>
  </verify>
  <done>All 9 algorithms pass to_dot() tests at both detail levels. DOT strings contain parameter values. Node counts are within spec (5-10 block, 15-30 component).</done>
</task>

</tasks>

<verification>
- `pytest tests/test_signal_flow.py -x -q` -- all tests pass
- Every algorithm in ALGORITHM_REGISTRY returns valid DOT at both levels
- DOT strings render without errors when pasted into a Graphviz viewer
</verification>

<success_criteria>
- All 9 algorithms have to_dot() returning valid Graphviz DOT strings
- Block-level diagrams have 5-10 nodes showing major DSP stages
- Component-level diagrams have 15-30 nodes showing individual elements
- Parameter values appear in diagram labels
- Shared dot_builder helpers used consistently across all algorithms
</success_criteria>

<output>
After completion, create `.planning/phases/09-signal-flow-diagrams-c-export/09-01-SUMMARY.md`
</output>
