---
phase: 09-signal-flow-diagrams-c-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - claudeverb/algorithms/freeverb.py
  - claudeverb/algorithms/dattorro_plate.py
  - claudeverb/export/c_export.py
  - tests/test_c_export.py
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04

must_haves:
  truths:
    - "Every registered algorithm can be exported to .h and .c file content"
    - "Exported C code includes current parameter settings as comments and init defaults"
    - "RAM estimation returns per-component breakdown with SRAM/SDRAM fit indicators"
    - "AudioCallback template includes Hothouse hardware init, knob reads, and process call"
  artifacts:
    - path: "claudeverb/export/c_export.py"
      provides: "C code generation: generate_header, generate_source, generate_audio_callback, estimate_ram"
      min_lines: 120
      exports: ["generate_header", "generate_source", "generate_audio_callback", "estimate_ram"]
    - path: "tests/test_c_export.py"
      provides: "Test coverage for all export functions across algorithms"
      min_lines: 80
    - path: "claudeverb/algorithms/freeverb.py"
      provides: "to_c_struct() and to_c_process_fn() methods"
      contains: "def to_c_struct"
    - path: "claudeverb/algorithms/dattorro_plate.py"
      provides: "to_c_struct() and to_c_process_fn() methods"
      contains: "def to_c_struct"
  key_links:
    - from: "claudeverb/export/c_export.py"
      to: "claudeverb/algorithms/*.py"
      via: "calls algo.to_c_struct() and algo.to_c_process_fn()"
      pattern: "to_c_struct|to_c_process_fn"
    - from: "claudeverb/export/c_export.py"
      to: "claudeverb/algorithms/base.py"
      via: "uses algo.param_specs for parameter export"
      pattern: "param_specs"
    - from: "tests/test_c_export.py"
      to: "claudeverb/export/c_export.py"
      via: "imports and tests all export functions"
      pattern: "from claudeverb\\.export\\.c_export import"
---

<objective>
Build the C code export pipeline: add missing to_c_struct()/to_c_process_fn() to Freeverb and DattorroPlate, then create the centralized c_export.py module with header generation, source generation, RAM estimation, and Hothouse AudioCallback template.

Purpose: Completes the data layer for all EXP requirements. UI integration comes in Plan 03.

Output: All 9 algorithms exportable to C, centralized export module, full test coverage.
</objective>

<execution_context>
@/Users/jasondoherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jasondoherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-signal-flow-diagrams-c-export/09-CONTEXT.md
@.planning/phases/09-signal-flow-diagrams-c-export/09-RESEARCH.md

@claudeverb/algorithms/base.py
@claudeverb/algorithms/__init__.py
@claudeverb/algorithms/freeverb.py
@claudeverb/algorithms/dattorro_plate.py
@claudeverb/algorithms/fdn_reverb.py

<interfaces>
<!-- Existing C export pattern from fdn_reverb.py -->

From claudeverb/algorithms/fdn_reverb.py (line ~529):
```python
def to_c_struct(self) -> str:
    """Return C typedef struct for this algorithm's state."""
    return """\
typedef struct {
    float delay_buf_0[1265];
    // ... fixed-size buffers, scalars, write indices
    int sample_rate;
    int frozen;
    float width;
} FDNReverbState;
"""

def to_c_process_fn(self) -> str:
    """Return C process function for this algorithm."""
    return """\
void fdn_reverb_process(FDNReverbState* state, const float* input,
                        float* output_left, float* output_right,
                        int num_samples) {
    // ... sample-by-sample processing
}
"""
```

From claudeverb/algorithms/base.py:
```python
class ReverbAlgorithm(ABC):
    @property
    @abstractmethod
    def param_specs(self) -> dict: ...
```

Hothouse hardware (from research):
- 6 knobs: KNOB_1 through KNOB_6
- 3 toggle switches: TOGGLESWITCH_1 through TOGGLESWITCH_3
- 2 footswitches, 2 LEDs
- Uses hothouse.h + daisysp.h includes
- Daisy Seed: 512 KB SRAM, 64 MB SDRAM
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add to_c_struct() and to_c_process_fn() to Freeverb and DattorroPlate, create test scaffold</name>
  <files>claudeverb/algorithms/freeverb.py, claudeverb/algorithms/dattorro_plate.py, tests/test_c_export.py</files>
  <behavior>
    - Test: Freeverb.to_c_struct() returns string containing "typedef struct" and "FreeverbState"
    - Test: Freeverb.to_c_process_fn() returns string containing "void freeverb_process"
    - Test: DattorroPlate.to_c_struct() returns string containing "typedef struct" and "DattorroPlateState"
    - Test: DattorroPlate.to_c_process_fn() returns string containing "void dattorro_plate_process"
    - Test: All 9 algorithms in ALGORITHM_REGISTRY have to_c_struct() and to_c_process_fn() methods
    - Test: No generated C contains malloc, calloc, or realloc (C portability rule)
    - Test: All delay buffers are fixed-size arrays with compile-time constant sizes
  </behavior>
  <action>
1. Create `tests/test_c_export.py` with test cases for the behaviors above. Include parametrized tests iterating all ALGORITHM_REGISTRY entries.

2. Add `to_c_struct()` to Freeverb following the existing pattern from fdn_reverb.py:
   - Include all 8 comb filter delay buffers (fixed sizes matching Freeverb's COMB_TUNING values)
   - Include all 4 allpass filter delay buffers (fixed sizes matching ALLPASS_TUNING values)
   - Include write indices, filter state variables (filterstore for each comb)
   - Include parameter scalars: room_size, damp, wet, dry, width, mode, frozen
   - Name: FreeverbState

3. Add `to_c_process_fn()` to Freeverb:
   - Process function: `void freeverb_process(FreeverbState* state, const float* input, float* output_left, float* output_right, int num_samples)`
   - Sample-by-sample loop: sum input, process 8 combs in parallel, sum outputs, chain through 4 allpasses, stereo spread

4. Add `to_c_struct()` to DattorroPlate following the same pattern:
   - Include all delay buffers for 4 input diffusers, tank delay lines, modulated allpasses
   - Include LFO state (phase, rate), damping filter states, DC blocker states
   - Include parameter scalars: decay, damping, bandwidth, pre_delay, etc.
   - Name: DattorroPlateState

5. Add `to_c_process_fn()` to DattorroPlate:
   - Process function: `void dattorro_plate_process(DattorroPlateState* state, const float* input, float* output_left, float* output_right, int num_samples)`
   - Input diffusion chain, figure-eight tank loop, multi-tap output

All buffers must be fixed-size (compile-time constants). No malloc/calloc. Follow existing codebase patterns exactly.
  </action>
  <verify>
    <automated>cd /Users/jasondoherty/Documents/SRC/claudeverb/.claude/worktrees/jovial-leakey && pytest tests/test_c_export.py -x -q -k "struct or process_fn or no_malloc"</automated>
  </verify>
  <done>All 9 algorithms have to_c_struct() and to_c_process_fn(). No dynamic allocation in any generated C code. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create c_export.py with header generation, source generation, RAM estimation, and AudioCallback template</name>
  <files>claudeverb/export/c_export.py, tests/test_c_export.py</files>
  <behavior>
    - Test: generate_header() returns string with #ifndef guard, struct typedef, init/process prototypes, param comments
    - Test: generate_source() returns string with #include, init function setting defaults, process function body
    - Test: estimate_ram() returns dict with delay_lines_kb, filters_kb, total_kb, fits_sram, fits_sdram keys
    - Test: estimate_ram() correctly counts float arrays from to_c_struct() output
    - Test: generate_audio_callback() returns string with hothouse.h include, AudioCallback function, knob reads, main()
    - Test: generate_audio_callback() knob mapping uses provided mapping dict
    - Test: Current param values appear in header comments and init function defaults
  </behavior>
  <action>
Create `claudeverb/export/c_export.py` with these functions (using Python f-strings per user decision, NOT Jinja2):

1. **`generate_header(algo, params: dict, knob_mapping: dict) -> str`**:
   - `#ifndef ALGONAME_H` / `#define` / `#endif` include guards
   - Header comment block with algorithm name, export timestamp, current parameter values
   - `algo.to_c_struct()` typedef
   - `void algoname_init(AlgoNameState* state);` prototype
   - `void algoname_process(AlgoNameState* state, const float* input, float* output_left, float* output_right, int num_samples);` prototype
   - Helper `_snake_case(name)` for function naming

2. **`generate_source(algo, params: dict) -> str`**:
   - `#include "AlgoName.h"`
   - `void algoname_init(AlgoNameState* state)` function that sets all scalar params to current values from `params` dict, zeros all buffers, sets write indices to 0
   - `algo.to_c_process_fn()` body (already provided by each algorithm)
   - Current parameter values as comments alongside each default assignment

3. **`estimate_ram(algo) -> dict`**:
   - Parse `algo.to_c_struct()` output with regex to find `float name[N]` and `int name[N]` declarations
   - Count float/int scalars
   - Calculate bytes: float arrays * 4, int arrays * 4, scalars * 4
   - Add 10% padding for struct alignment
   - Return dict: `delay_lines_kb`, `filters_kb`, `matrices_kb`, `total_kb`, `fits_sram` (< 512 KB), `fits_sdram` (< 64 MB)
   - Include `sdram_candidates`: list of buffer names > 4096 floats (16 KB) that should use DSY_SDRAM_BSS

4. **`generate_audio_callback(algo, knob_mapping: dict) -> str`**:
   - `#include "daisysp.h"`, `#include "hothouse.h"`, `#include "AlgoName.h"`
   - `using clevelandmusicco::Hothouse;`
   - Global state: `Hothouse hw;`, `AlgoNameState state;`, `Parameter knobs[6];`, `bool bypass = true;`
   - `void AudioCallback(...)` with `hw.ProcessAllControls()`, knob reads mapped per knob_mapping, bypass check, process call
   - `int main(void)` with `hw.Init()`, `hw.SetAudioBlockSize(48)`, `hw.SetAudioSampleRate(SAI_48KHZ)`, `algoname_init(&state)`, knob parameter inits with min/max from param_specs, `hw.StartAudio(AudioCallback)`, `while(1) {}`
   - Build instructions as header comment: toolchain (arm-none-eabi-gcc), required includes, flash command hint
   - Knob mapping uses param_specs min/max for `Parameter::Init()` range arguments

5. **`export_to_files(algo, params: dict, knob_mapping: dict, output_dir: str = "daisyexport") -> dict`**:
   - Creates output_dir if not exists
   - Writes AlgoName.h and AlgoName.c
   - Returns dict with file paths and RAM estimate

Add remaining tests to `tests/test_c_export.py` for all the behaviors above.
  </action>
  <verify>
    <automated>cd /Users/jasondoherty/Documents/SRC/claudeverb/.claude/worktrees/jovial-leakey && pytest tests/test_c_export.py -x -q</automated>
  </verify>
  <done>c_export.py exports all 5 functions. All tests pass. RAM estimation matches manual calculation within 10%. AudioCallback template includes all Hothouse hardware setup. Parameter values appear in generated code.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_c_export.py -x -q` -- all tests pass
- All 9 algorithms exportable (no AttributeError on to_c_struct/to_c_process_fn)
- Generated .h files have include guards, struct, prototypes
- Generated .c files have init function with param defaults, process function
- RAM estimation returns reasonable KB values for known algorithms
- AudioCallback includes Hothouse-specific hardware init
</verification>

<success_criteria>
- Freeverb and DattorroPlate have working to_c_struct() and to_c_process_fn()
- c_export.py generates complete .h and .c file content for any algorithm
- Current parameter values appear as comments and defaults in generated code
- RAM estimation correctly parses struct definitions and returns KB breakdown
- AudioCallback template is ready-to-compile with Hothouse hardware setup
- No dynamic allocation (malloc/calloc) in any generated C code
</success_criteria>

<output>
After completion, create `.planning/phases/09-signal-flow-diagrams-c-export/09-02-SUMMARY.md`
</output>
