---
phase: 04-engine-streamlit-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - claudeverb/streamlit_app.py
autonomous: true
gap_closure: true
requirements:
  - AIO-03

must_haves:
  truths:
    - "Audio players for input, output, and impulse response all loop automatically in the browser"
  artifacts:
    - path: "claudeverb/streamlit_app.py"
      provides: "Streamlit web application with looping audio players"
      contains: "loop=True"
  key_links:
    - from: "claudeverb/streamlit_app.py"
      to: "st.audio (lines 177, 182, 245)"
      via: "loop=True keyword argument"
      pattern: "st\\.audio.*loop=True"
---

<objective>
Close the single AIO-03 gap identified in the Phase 04 verification report: three `st.audio` calls in `claudeverb/streamlit_app.py` omit `loop=True`, so the browser audio players do not loop. Each fix is a one-line change.

Purpose: Satisfy AIO-03 ("User can play, stop, and loop processed audio output") fully.
Output: `claudeverb/streamlit_app.py` with `loop=True` on all three `st.audio` calls (lines 177, 182, 245).
</objective>

<execution_context>
@/Users/jasondoherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jasondoherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engine-streamlit-ui/04-VERIFICATION.md

<interfaces>
<!-- Current st.audio calls that need loop=True added. -->

Line 177 (Input audio player):
```python
st.audio(dry, sample_rate=SAMPLE_RATE)
```
Becomes:
```python
st.audio(dry, sample_rate=SAMPLE_RATE, loop=True)
```

Line 182 (Output audio player, blended wet/dry):
```python
st.audio(blended, sample_rate=SAMPLE_RATE)
```
Becomes:
```python
st.audio(blended, sample_rate=SAMPLE_RATE, loop=True)
```

Line 245 (Impulse response audio player):
```python
st.audio(ir, sample_rate=SAMPLE_RATE)
```
Becomes:
```python
st.audio(ir, sample_rate=SAMPLE_RATE, loop=True)
```

Note: Streamlit 1.55.0 (installed) supports the `loop` keyword argument on `st.audio`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add loop=True to all three st.audio calls</name>
  <files>claudeverb/streamlit_app.py</files>
  <action>
    Open `claudeverb/streamlit_app.py` and make three targeted edits:

    1. Line 177: Change `st.audio(dry, sample_rate=SAMPLE_RATE)` to `st.audio(dry, sample_rate=SAMPLE_RATE, loop=True)`
    2. Line 182: Change `st.audio(blended, sample_rate=SAMPLE_RATE)` to `st.audio(blended, sample_rate=SAMPLE_RATE, loop=True)`
    3. Line 245: Change `st.audio(ir, sample_rate=SAMPLE_RATE)` to `st.audio(ir, sample_rate=SAMPLE_RATE, loop=True)`

    No other changes. The rest of the file is verified correct and must not be touched.
  </action>
  <verify>
    <automated>grep -c "loop=True" claudeverb/streamlit_app.py</automated>
  </verify>
  <done>grep reports exactly 3 occurrences of `loop=True` in streamlit_app.py. All existing tests still pass: `pytest tests/test_streamlit_app.py tests/test_engine.py -x -q` exits 0.</done>
</task>

</tasks>

<verification>
- `grep -c "loop=True" claudeverb/streamlit_app.py` outputs `3`
- `pytest tests/test_streamlit_app.py tests/test_engine.py -x -q` -- all 19 tests pass, no regressions
- `grep "st.audio" claudeverb/streamlit_app.py` -- every st.audio call includes `loop=True`
</verification>

<success_criteria>
- All three `st.audio` calls in `claudeverb/streamlit_app.py` include `loop=True`
- AIO-03 fully satisfied: audio players support play, stop, and loop
- Existing 19 Phase 04 tests continue to pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-engine-streamlit-ui/04-03-SUMMARY.md`
</output>
